# 🔍 テロップ調整検証システム (Lv3 詳細仕様)

## ✨ TL;DR
- テロップ調整の **3段階検証ロジック**: 初期判定 → 優先順位調整実行 → 再検証 + 最終検証。
- `verify_fit()` 関数で「本当にはみ出さないか」を複数回確認。
- Pillow の `ImageFont.getbbox()` で各行を個別計測。
- min_fontsize=40、スケーリング係数=0.85 で小さくなりすぎを防止。
- 36字以上の長文は警告ログ + ユーザーへの短縮提案。
- 実装ファイル：[scripts/adjust_tickers.py](../../../scripts/adjust_tickers.py), [src/render/ffmpeg_runner.py](../../../src/render/ffmpeg_runner.py)。

## 📚 用語・前提
- **Pillow bbox**: `font.getbbox(text)` で返される (x0, y0, x1, y1)。幅 = x1 - x0。
- **max_width**: 画面幅の 90%（マージン 10% 確保）。1920x1080 なら 1728px、1080x1920 なら 972px。
- **verify_fit()**: 最終検証関数。テキスト+フォント+サイズで「はみ出すか」を判定。
- **scale_factor**: 最後の手段。全体を 0.85 倍に縮小。
- **min_fontsize**: 下限値。40pt で視認性と調整性のバランスを取る。

## 🧭 背景
- 優先順位1-3の調整をしても、改行位置の計算誤差や Pillow 計測のズレで、再度はみ出す可能性がある。
- 複数行テキストの場合、最長行だけではなく全行をチェックが必要。
- ユーザーが手動短縮を検討できるよう、36字以上で明示的に警告を表示する。

## 🏗️ 実装詳細

### verify_fit() 関数（[scripts/adjust_tickers.py](../../../scripts/adjust_tickers.py) 行 15-40）

```python
def verify_fit(text: str, font_path: str, fontsize: int, max_width: int) -> Tuple[bool, float]:
    """
    テキストが max_width に収まるかを検証。複数行対応。
    
    戻り値: (is_fit: bool, max_line_width: float)
      - is_fit: True なら収まっている
      - max_line_width: 最長行の幅（px）
    """
    try:
        from PIL import ImageFont, Image
    except Exception as e:
        logger.error(f"Pillow import failed: {e}")
        return True, 0  # デフォルト安全側（skip）

    try:
        font = ImageFont.truetype(font_path, size=fontsize)
        lines = text.split("\n")
        
        max_width_found = 0
        for idx, line in enumerate(lines):
            if not line.strip():
                continue
            try:
                # 各行を個別に計測
                bbox = font.getbbox(line)
                line_width = bbox[2] - bbox[0]  # x1 - x0
                max_width_found = max(max_width_found, line_width)
                
                if line_width > max_width:
                    logger.debug(f"Line {idx} exceeds: '{line[:20]}...' = {line_width}px > {max_width}px")
            except Exception as e:
                logger.warning(f"getbbox failed for line: {line[:20]}..., error: {e}")
                return False, 0
        
        return max_width_found <= max_width, max_width_found
    
    except Exception as e:
        logger.error(f"verify_fit error: {e}")
        return False, 0
```

### adjust_section() 改良版（行 50-150）

```python
def adjust_section(
    section,
    video_width: int,
    min_fontsize: int = 40,
    scale_factor: float = 0.85
) -> bool:
    """
    セクションのテロップを3段階優先順位で調整し、複数回検証。
    
    戻り値: True なら変更あり、False なら変更なし
    """
    ensure_segments(section)
    changed = False
    max_width = int(video_width * 0.9)
    
    for idx, seg in enumerate(section.on_screen_segments):
        # Pydantic オブジェクト or dict に対応
        if hasattr(seg, "model_dump"):
            seg_dict = seg.model_dump()
            seg_is_model = True
        elif isinstance(seg, dict):
            seg_dict = seg
            seg_is_model = False
        else:
            continue

        text = seg_dict.get("text") or ""
        style_dict = seg_dict.get("style") or {}
        
        # デフォルト補完
        if not style_dict.get("font"):
            style_dict["font"] = "Noto Sans JP"
        if "fontsize" not in style_dict or style_dict.get("fontsize") in (None, 0, ""):
            style_dict["fontsize"] = 64
        
        style = TextStyle.model_validate(style_dict)
        font_path = _resolve_font_path(style.font)
        orig_fontsize = style.fontsize or 64
        current_fontsize = orig_fontsize
        current_text = text
        
        # ===== 段階1: 初期判定 =====
        is_fit, max_line_width = verify_fit(current_text, font_path, current_fontsize, max_width)
        
        if is_fit:
            logger.info(f"✅ Segment OK (no adjustment needed): '{text[:20]}...'")
            # 修正なし、次へ
            continue
        
        logger.info(f"⚠️ Segment needs adjustment: '{text[:20]}...' ({max_line_width}px > {max_width}px)")
        
        # ===== 段階2: 優先順位実行 =====
        
        # 優先順位1: フォントサイズ縮小
        logger.debug(f"[P1] Trying font size reduction...")
        while current_fontsize > min_fontsize:
            current_fontsize = max(min_fontsize, int(round(current_fontsize * 0.9)))
            is_fit, line_width = verify_fit(current_text, font_path, current_fontsize, max_width)
            if is_fit:
                logger.info(f"✅ [P1] Font size OK: {current_fontsize}pt")
                changed = True
                break
        else:
            # min_fontsize でもはみ出す場合
            logger.debug(f"[P1] Font size reached minimum ({min_fontsize}pt), still not fit. Trying P2...")
            
            # 優先順位2: 改行挿入
            logger.debug(f"[P2] Trying line wrapping...")
            wrapped = split_text_for_wrap(current_text)
            is_fit, line_width = verify_fit(wrapped, font_path, current_fontsize, max_width)
            
            if is_fit:
                current_text = wrapped
                logger.info(f"✅ [P2] Line wrapping OK")
                changed = True
            else:
                # 改行してもダメなら、再度フォント縮小
                logger.debug(f"[P2] Wrapped text still too long, retry P1 with wrapped text...")
                while current_fontsize > min_fontsize:
                    current_fontsize = max(min_fontsize, int(round(current_fontsize * 0.9)))
                    is_fit, _ = verify_fit(wrapped, font_path, current_fontsize, max_width)
                    if is_fit:
                        current_text = wrapped
                        logger.info(f"✅ [P1+P2] Font + wrapping OK: {current_fontsize}pt")
                        changed = True
                        break
                else:
                    # 優先順位3: スケーリング
                    logger.debug(f"[P3] Trying scaling (0.85x)...")
                    scaled_fontsize = int(current_fontsize * scale_factor)
                    is_fit, line_width = verify_fit(current_text, font_path, scaled_fontsize, max_width)
                    
                    if is_fit:
                        current_fontsize = scaled_fontsize
                        logger.info(f"✅ [P3] Scaling OK: {current_fontsize}pt")
                        changed = True
                    else:
                        # 全て失敗
                        logger.warning(f"❌ All adjustments failed: '{text[:30]}...' ({len(text)} chars)")
                        if len(text) >= 36:
                            logger.warning(f"💡 [Suggestion] Text is too long ({len(text)} chars). Consider shortening to <18 chars.")
        
        # ===== 段階3: 最終検証 =====
        logger.debug(f"[Stage3] Final verification...")
        is_fit_final, final_width = verify_fit(current_text, font_path, current_fontsize, max_width)
        
        if not is_fit_final:
            logger.error(f"🚨 Final verification FAILED: '{current_text[:30]}...' ({final_width}px > {max_width}px)")
        else:
            logger.info(f"✅ Final verification PASSED: {current_fontsize}pt")
        
        # ===== YAML に反映 =====
        style.fontsize = current_fontsize
        seg_dict["text"] = current_text
        seg_dict["style"] = style.model_dump()
        
        # 反映
        if seg_is_model:
            seg.text = current_text
            seg.style = style.model_dump()
        else:
            section.on_screen_segments[idx] = OnScreenSegment(**seg_dict)
    
    return changed
```

### split_text_for_wrap() ヘルパー関数（行 155-180）

```python
def split_text_for_wrap(text: str, preferred_break: str = None) -> str:
    """
    テキストをスペース/句読点で改行。日本語対応。
    
    Args:
        text: 元テキスト
        preferred_break: 優先的に分割する区切り文字（デフォルト: "。、")
    
    戻り値: 改行挿入版のテキスト
    """
    if len(text) <= 20:
        return text  # 短い場合は改行不要
    
    # 優先度順に分割候補を探す
    delimiters = preferred_break.split() if preferred_break else ["。", "、", " ", "，", "！", "？"]
    
    for delim in delimiters:
        if delim in text:
            # 最も中央に近い位置で分割
            idx = text.find(delim)
            if idx > 0 and idx < len(text) - 1:
                return text[:idx + len(delim)] + "\n" + text[idx + len(delim):]
    
    # 分割できない場合は、文字の中央で分割
    mid = len(text) // 2
    return text[:mid] + "\n" + text[mid:]
```

## 🔧 運用・デバッグ

### CLI でテスト
```bash
# テロップ調整 + ログ出力
python scripts/adjust_tickers.py \
  --script inputs/scripts_yaml/test.yaml \
  --output inputs/scripts_yaml/adjusted_test.yaml \
  --log-level DEBUG
```

### ログ出力例
```
⚠️ Segment needs adjustment: 'これは非常に長いテロップのサンプルです' (1850px > 1728px)
[P1] Trying font size reduction...
✅ [P1] Font size OK: 56pt
✅ Final verification PASSED: 56pt
```

### テスト用 YAML
```yaml
sections:
  - id: test1
    on_screen_text: "短いテロップ"  # ← 18字以下、修正なし
    
  - id: test2
    on_screen_text: "これはちょっと長めのテロップです。19から35字くらい。"  # ← 改行 or 縮小
    
  - id: test3
    on_screen_text: "これは非常に長いテロップのサンプルです。36字以上で短縮提案が出ます。短くしてください。"  # ← 警告
```

### verify_fit() の検証
```python
from PIL import ImageFont
from scripts.adjust_tickers import verify_fit

font_path = "/System/Library/Fonts/HiraKakuProN-W4.otf"
fontsize = 64
max_width = 1728

# テスト1: 短いテキスト
is_fit, width = verify_fit("短い", font_path, fontsize, max_width)
print(f"Test1: {is_fit}, width={width}px")  # True, ~100px

# テスト2: 長いテキスト
is_fit, width = verify_fit("これは非常に長いテロップのサンプルです", font_path, fontsize, max_width)
print(f"Test2: {is_fit}, width={width}px")  # False, ~1800px

# テスト3: 改行テキスト
is_fit, width = verify_fit("これは非常に長い\nテロップのサンプルです", font_path, fontsize, max_width)
print(f"Test3: {is_fit}, width={width}px")  # True, ~900px
```

## 🔗 参考
- **実装ファイル**: 
  - [scripts/adjust_tickers.py](../../../scripts/adjust_tickers.py)
  - [src/render/ffmpeg_runner.py](../../../src/render/ffmpeg_runner.py) (`_fit_font_size()`)
- **テスト**: [tests/test_text_position_format.py](../../../tests/test_text_position_format.py)
- **Pillow ドキュメント**: https://pillow.readthedocs.io/en/stable/reference/ImageFont.html
- **FFmpeg drawtext**: https://ffmpeg.org/ffmpeg-filters.html#drawtext-1

## ✅ まとめ
- `verify_fit()` で複数回検証し、本当にはみ出さないことを保証。
- 3段階優先順位：①文字サイズ縮小 → ②改行 → ③スケーリング。
- 複数行は全行をチェック。最長行を基準に判定。
- min_fontsize=40 で視認性を確保。
- 36字以上は警告＋短縮提案をログ出力。
- 最終検証で YAML 保存前に確認。

## 🚀 次のアクション
- 実装の CI/CD パイプライン統合テスト。
- 言語別（中文、韓文、タイ文字等）の計測精度テスト。
- GUI でテロップ調整の実時間プレビュー表示。
- キャッシング機構で大規模台本の処理高速化。

## 🗓️ 追記/更新ログ
- 2025-12-23: 初版。Lv3 詳細仕様として作成。verify_fit(), 3段階検証ロジック記載。
